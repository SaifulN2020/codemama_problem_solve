void main() {
  double screenWidth = 600;

  // Table width is fixed at 300 pixels
  double tableWidth = 300;

  // Calculate the horizontal position
  double horizontalPosition = (screenWidth - tableWidth) / 2;

  // Output the result
  print("Horizontal Position of the Table: $horizontalPosition pixels");
}